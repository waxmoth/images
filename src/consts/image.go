package consts

const (
	// HandleCrop the parameter to check it is use crop image method
	HandleCrop = `crop`
	// HandleResize the parameter to check it is resize image or not
	HandleResize = `resize`
)
